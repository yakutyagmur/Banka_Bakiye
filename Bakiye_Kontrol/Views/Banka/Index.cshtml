@model IEnumerable<Bakiye_Kontrol.Models.Entity.Banka>

@{
    ViewBag.Title = "Index";
}
<div style="background-color:azure" class="container">
    <h2 style="font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; color: #3c3b8b">Banka</h2>
    <table style="background-color:khaki;border-radius:4.2vh" class="table table-hover" class="table">
        <thead>
            <tr>
                <th style="text-align:center">
                    Banka Kodu
                </th>
                <th>
                    Banka Adı
                </th>
                <th>
                    Şube Adı
                </th>
                <th>
                    Hesap Numarası
                </th>
                <th>
                    Iban
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td style="text-align:center">
                        @Html.DisplayFor(modelItem => item.BankaKodu)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.BankaAdi)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SubeAdi)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.HesapNo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Iban)
                    </td>
                    <td>
                        @Html.ActionLink("Düzenle", "Edit", new { id = item.ID }, new { @class = "btn btn-warning" })
                        @Html.ActionLink("Sil", "Delete", new { id = item.ID }, new { @class = "btn btn-danger" })
                    </td>
                </tr>
            }
        </tbody>

    </table>
    <p>
        @Html.ActionLink("Ekle", "Create", null, new { id = "OT", style = "color:white", @class = "btn btn-success" })
    </p>
</div>
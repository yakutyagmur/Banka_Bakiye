@model IEnumerable<Bakiye_Kontrol.Models.Entity.Kasa>

@{
    ViewBag.Title = "Index";
}
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div style="background-color:azure" class="container">
        <h2 style="font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; color: #3c3b8b">Kasa</h2>
        <table style="background-color:khaki;border-radius:4.2vh" class="table table-hover" class="table">
            <thead>
                <tr>
                    <th style="text-align:center">
                        Kasa Kodu
                    </th>
                    <th>
                        Kasa Adı
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td style="text-align:center">
                            @Html.DisplayFor(modelItem => item.KasaKodu)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.KasaAdi)
                        </td>
                        <td>
                            @Html.ActionLink("Düzenle", "Edit", new { id = item.ID }, new { @class = "btn btn-warning" })
                            @Html.ActionLink("Sil", "Delete", new { id = item.ID }, new { @class = "btn btn-danger" })
                        </td>
                    </tr>
                }
            </tbody>

        </table>

        <p>
            @*<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#Modal1">
                    Ekle
                </button>
                <div class="modal" id="Modal1">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h2 class="modal-title">Kasa Bilgileri</h2>
                            </div>
                            <form method="post" action="~/Kasas/Ekle">
                                <div class="modal-body">
                                    <label>
                                        Kasa Kodu
                                    </label>
                                    <input type="text" name="Kasa Kodu" class="form-control"/>
                                    <br />
                                    <label>
                                        Kasa Adı
                                    </label>
                                    <input type="text" name="Kasa Adı" class="form-control"/>
                                    <br />
                                    <button class="btn btn-info">EKLE</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>*@
            @Html.ActionLink("Ekle", "Create", null, new { id = "OT", style = "color:white", @class = "btn btn-success" })
        </p>

    </div>
}